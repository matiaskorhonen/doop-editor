// Umbrella header for TextStory's `Internal` ObjC target, which TextStory re-exports
// through public typealiases. Xcode only generates a framework module map when a public
// header matching the module name exists.
#import "TSYTextStorage.h"
